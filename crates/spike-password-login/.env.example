# Copy to `.env` (same directory) and fill in your real values.
# `.env` is gitignored and never committed; real environment variables
# always take precedence over values set here.
MAGISTER_SCHOOL="schoolnaam zoals in Magister"
MAGISTER_USERNAME="gebruikersnaam"
MAGISTER_PASSWORD="wachtwoord"
